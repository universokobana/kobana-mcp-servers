[< Story](index.md)

# Plan — <CODE> <Title>

## Pull

<Pulled item and why this level now.>

## Prepare

<Context summary, story shape, risks, and applicable rules.>

## Scope

- <in scope>

## Non-Goals

- <out of scope>

## Design Decisions

<Plan-local design decisions, when the plan needs to settle some. Label them
`DD1`, `DD2`, ... — **never** `MD1`, `MD2`. A bare `MD<n>` is reserved for this
repository's decision registry in `docs/project/decisions/`, so a plan that labels
its own decisions `MD1` collides with a real decision code.

This repository is **public**. Decisions belonging to the Kia registry are cited by
bare code (`D109`, `BD004`) and never linked, because their documents live in
internal repositories — a link would 404 for outside readers and leak internal
structure.>

## Implementation Approach

<How implementation will proceed.>

## Test Strategy

<Automated and manual tests.>

## Validation Route

<How the Navigator validates observable behavior, when applicable.>

## Checkpoint

Implementation must not start until the Navigator approves this plan.
