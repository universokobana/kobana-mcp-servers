[< Roadmap](index.md)

# Technical Debt Ledger

Ariad Review records technical debt here when debt should be paid now or deferred.

Seeded on 2026-09-12 at Ariad adoption, from the survey that accompanied the
timeout-fence work. Every line names the debt instead of letting it become drift.

| ID | Source | Location | Kind | Description | Impact | Recommendation | Navigator Decision | Status |
|----|--------|----------|------|-------------|--------|----------------|--------------------|--------|
| TD-001 | PR #11 (timeout port) | `mcp-{admin,charge,data,edi,financial,mailbox,payment,transfer}/src/api/client.ts` | Duplication | Eight near-identical copies of `KobanaApiClient`. One incident on 2026-08-24 required the same patch eight times; the port was only safe because the seven target regions were *proved* byte-identical and applied by an asserting script | Every future client fix pays the same ritual, and every client test would otherwise be written eight times | Extract a shared `core` package consumed as a workspace dependency — but only after analyzing the vendoring path, since `kia-desktop` installs these packages with `--ignore-scripts` into a byte-reproducible tree | **Accepted** — decided 2026-09-12 ([MD-005](../decisions/md005-client-duplication-accepted/index.md)): duplication stays for now; CV2 works around it by parametrizing tests over packages rather than duplicating them | Open |
| TD-002 | PR #11 (out of scope) | `mcp-help/src/api/client.ts` | Correctness | `HelpCenterClient.fetchPage` has no timeout — the same unbounded hang the other packages were just fenced against. Different class, different config shape (no token, no `apiUrl`), so it needs its own error type rather than `KobanaApiTimeoutError` | **Live in `kadu`**, which runs `kobana-mcp-help` via `npx -y`. A stalled help-center response hangs the tool call indefinitely | Fence with its own budget and error type | Deferred — needs a budget decision | Open |
| TD-003 | PR #11 (out of scope) | `mcp-site/src/api/cache-manager.ts:95` | Correctness | `await fetch(zipUrl)` then `arrayBuffer()` downloads a ZIP with no timeout | **Live in `kadu`**, which runs `kobana-mcp-site` via `npx -y`. Unlike the API clients, a 30s budget here would false-positive on a slow but healthy transfer, so the fix is not a copy of the others | Fence with a deliberately larger, separately reasoned budget | Deferred — needs a budget decision | Open |
| TD-004 | 2026-09-12 survey | `src/server.ts`, `src/http-server.ts` (×2), `src/api/client.ts` `User-Agent` | Correctness | Version is hardcoded `1.0.0` in three places per package plus the `User-Agent`, while `package.json` reads the real version (`1.2.0` for financial) | A consumer inspecting the MCP handshake is told the wrong version; debugging a version-specific bug starts from a false premise | Read the version from `package.json` and assert lockstep in a test | Pay now — it is [CV1.DS1](cv1-release-and-consumers/cv1-ds1-version-truth-and-publish/index.md) | Open |
| TD-005 | 2026-09-12 survey | 9 × `package-lock.json` | Build integrity | Nine per-package lockfiles coexist with an npm `workspaces` declaration, which expects a single root lockfile | Almost certainly why GitHub reports 532 advisories while the root tree reports 7 — Dependabot scans all nine trees. Two sources of dependency truth | Consolidate to one root lockfile | Deferred to CV3.DS4 | Open |
| TD-006 | 2026-09-12 survey | root `package.json` `workspaces` | Build integrity | `mcp-help` and `mcp-site` are absent from `workspaces`, so root `npm run build` skips them entirely | Two published packages are outside every root-level command, including any future CI gate — the two that are also unfenced (TD-002, TD-003) | Include them, or document why they are deliberately outside | Deferred to CV3.DS4 | Open |
| TD-007 | 2026-09-12 survey | `kia-desktop/src-tauri/resources/mcp-packages/overrides/kobana-mcp-financial/` | Cross-repo | A downstream repository carries a committed override of this repository's built files, because upstream publishing "was not on our timeline" | A consumer is maintaining a patch of our artifact. Its version guard intentionally fails the build when we publish, so the debt is self-announcing | Delete the override directory once a version carrying the fence is published | Pay now — closed by [CV1.DS1](cv1-release-and-consumers/cv1-ds1-version-truth-and-publish/index.md) | Open |
| TD-008 | 2026-09-12 survey | `mcp-*/src/config.ts` | Dead code | `getConfigSafe()` is exported by every package's config; whether each package actually reaches it is unverified | Unused exports age silently and mislead readers — the class `knip` exists to catch | Audit consumption; delete or justify | Deferred to CV3.DS5 | Open |
| TD-009 | 2026-09-12 survey | tool names, descriptions and schemas across all packages | Cross-repo contract | 117 of the 122 tools in `kia-desktop`'s eval field catalog originate here, carried verbatim. A description edit changes model selection behavior measured downstream, but nothing in this repository marks those strings as behavior | A "copy tweak" can move another repository's measured curve with no signal at either end | State the practice in the principles (done) and decide whether a gate is warranted | Noted | Open |
